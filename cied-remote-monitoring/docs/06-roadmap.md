# 06. ロードマップ・次アクション

## 6.1 次にやること（2026/08/20 時点）

### 現在地

設計は固まった。**次にやることは「PDFを開いて全選択・コピーし、項目名を目視で拾う」という手作業。**
Python環境のセットアップは不要（下記「6.1-E」参照）。
画面イメージは [mockups/ui-mockup.html](../mockups/ui-mockup.html) で確認できる（5画面・ダミーデータ）。

- `python -m cli.list_labels`（`src/cli/list_labels.py`）を実装済みだが、**今は使わなくてよい。**
  数十件のPDFを一括処理する必要が出るM2以降まで着手を保留する（理由は6.1-E参照）
- ついでにオフラインPCへのPython持ち込み手順（`docs/08-offline-setup.md`）も作ったが、
  同じ理由で今は不要。**実行可能ファイルを閉域ネットワークに持ち込むこと自体が
  データ持ち込みよりリスクが高い**ため、必要になった時点で情報システム部門に確認してから進める

決まったこと:

| 項目 | 決定 |
|---|---|
| レポートの取得 | 各社システムから**PDFで直接取得できる**。印刷→スキャンは廃止 |
| ネットワーク | インターネット系と電子カルテ系は分離。**院内専用USB**で運ぶ |
| Phase 1 の形 | **本人1人での単独運用**（自分のPC 1台、localhost、認証なし） |
| 患者ID | **7桁の数字、先頭ゼロあり**。必ず文字列として扱う |
| システムの性格 | 遠隔モニタリング支援 **＋ 院内CIEDの台帳** |
| 主な画面 | 取り込み／確認・修正／カルテ転記／デバイス台帳／外来準備シート |

**当初の最大リスク（スキャン品質によるOCR精度不足）は解消している。**

---

### A. 現場で確認するだけのこと（コスト小・効果大）

**A-1. 各社PDFのテキストレイヤーの有無** ← 最優先

保存したPDFをビューアで開き、①本文の文字をドラッグして選択できるか、
②`Ctrl+F` で画面に見えている単語がヒットするか。両方できればテキストPDF。

| メーカー | PDF保存 | テキストレイヤー |
|---|---|---|
| Medtronic (CareLink) | ○ | 未確認 |
| Abbott (Merlin.net) | ○ | 未確認 |
| Boston Scientific (LATITUDE) | ○ | 未確認 |
| MicroPort | ○ | 未確認 |
| WCD (ZOLL LifeVest) | ○ | 未確認 |

全社テキストPDFなら、OCRは当面書かなくてよい。
画像PDFだったメーカーについてのみ、OCR経路を作り込む。

**A-2. カルテからコピーした「患者ID一覧」の実物**

外来準備シートの読み取りルールを実態に合わせるため。
見出し行・時刻・列の並びが分かればよい。
**患者IDと氏名はダミーに置き換えて**、形式だけ分かる状態で共有する。

**A-3. 既存の台帳（Excel等）があるか**

あれば、その列構成に合わせてCSV取込を作る。無ければ初期投入の方法を別途考える。
⚠️ Excelを経由すると患者IDの先頭ゼロが消える点に注意
（[01. FR-6](01-requirements.md#患者idの形式確定)）。

**A-4. レポートPDFを1〜3件ずつ手元に用意**

外部に渡す必要はない。自分のPC内で処理が完結する。

---

### B. 決めること（たたき台のレビュー）

いずれも現状は**推測で埋めてある**ので、実態に合わせて直す必要がある。

| # | 対象 | 場所 |
|---|---|---|
| B-1 | **カルテに転記したい項目の確定** | [04. データモデル](04-data-model.md) |
| B-2 | 台帳として管理したい項目（MRI可否・植込み施設・担当医・適応 など） | [04.2](04-data-model.md#42-台帳のデータモデル) |
| B-3 | 外来準備シートに出す項目（モックは12列） | モック ⑤ |
| B-4 | カルテ転記文の書き方（現物に寄せたい） | モック ③ |
| B-5 | **前回値との変化を「注意」とみなす幅**（`dir` / `tol`） | [01. FR-3](01-requirements.md#前回値との差分の可視化) |

> B-5 の数値（インピーダンス30%、閾値50% など）は私が置いた**仮の値**。
> 臨床的な妥当性は判断できないため、現場の感覚で決める必要がある。

---

### C. 情報システム部門に聞くこと

単独運用にしたことで、確認事項は2つだけになった。

| 確認事項 | 影響 |
|---|---|
| 自分のPCに実行ファイルをインストールしてよいか | 不可なら配布方法の再検討が必要 |
| **本システムと電子カルテを同じPCで開けるか** | 別端末だとクリップボード転記ができず手打ちに戻る。時間短縮効果が大きく削がれる |

Phase 2（複数人共有）に進むときは、サーバー設置・ポート開放・認証の確認が別途必要になる
（[02.3](02-architecture.md#23-配置構成)）。

---

### D. 測っておくこと

**現行の転記作業に1件あたり何分かかっているか。**
効果測定のベースラインになる。これが無いと M3 で「速くなったか」を言えない。

あわせて、遠隔なし患者が何名いるか（台帳の規模感）。

---

### E. 実装に入る条件と、最初の一歩

**A-1 と A-4 が揃えば M1 に着手できる。** B は並行して進めればよい。

#### 手順: ラベル一覧の取得（★方針転換: 手作業でよい）

1件〜数件のPDFから項目名を洗い出すだけなら、Pythonのセットアップは不要。
**PDFビューアで開いて全選択・コピーし、メモ帳やExcelに貼り付けるだけで十分**
（テキストPDFなので選択・コピーがそのまま使える。[3.2](03-extraction-design.md#32-a-テキストレイヤー優先重要)）。

1. 各社システムからレポートPDFを保存する（メーカーごとに1〜3件）← **未着手（A-4）**
2. テキストレイヤーの有無を確認する（A-1）← 上記のコピー操作ができれば、それ自体が確認になる
3. PDFビューアで全選択→コピー→メモ帳に貼り付け、項目名らしき行を目視でリストアップする
4. その一覧に患者の識別情報が混ざっていないか確かめる
5. その一覧をもとにプロファイルYAML（[04.4](04-data-model.md#44-表記ゆれの管理)）を作る ← 未着手

抽出ルールの作成に必要なのは帳票の**項目名**であって、患者の値ではない。
項目名は個人情報ではないため、そのまま扱える。**PDF本体を外部に出す必要はない。**

#### `cli.list_labels` スクリプトについて（実装済み・今は使わなくてよい）

`python -m cli.list_labels` を先に実装したが、これは**何十件ものPDFを一括処理する**
ような場面（M2以降、実運用に近づいてから）で効いてくるもの。
今のように数件を目視確認する段階では、上記の手作業のほうが速く、
Python環境の用意（オフラインPCへの持ち込み含む）というリスクも避けられる。
コードと `docs/08-offline-setup.md`（持ち込み手順）は残してあるが、**M2でバッチ処理が
必要になるまで着手を保留する。**

---

## 6.2 マイルストーン

### M0: 設計固め ← **ほぼ完了**

- [x] 要件・アーキテクチャ・抽出設計・データモデル・セキュリティの文書化
- [x] 画面モックアップの作成とレビュー
- [ ] 抽出項目・台帳項目の確定（上記 B）
- [ ] レポートPDFの用意（上記 A-4）

### M1: 抽出ルールの作成（Phase 0）

各社からPDFを直接取得できるため、**M1はOCR精度検証ではなく
「テキストPDFからの項目抽出ルール作成」**になった
（[03.2](03-extraction-design.md#決定-pdfの直接取得が可能)）。

- [x] `core` のデータ構造定義のうち `OcrToken` / `LabelCandidate`（`src/core/schema.py`）。
      `FieldValue` / `Report` / `DeviceRegistry` はラベルアンカー抽出エンジンの実装時に追加する
- [x] PDFテキスト抽出（`src/core/pdf.py`。座標は未対応、行テキストのみ）
- [x] **ラベル抽出スクリプト**（上記 E）→ `python -m cli.list_labels`（`src/cli/list_labels.py`）
      **未実機検証**。開発サンドボックスからPyPIへ到達できず `pypdfium2` をインストールできなかったため、
      `pip install -e ".[dev]"` の上、実PDFで必ず動作確認すること（下記「次にやること」参照）
- [ ] ラベルアンカー抽出エンジン
- [ ] 評価スクリプト（フィールド単位の正解率を測る）
- [ ] 1メーカー分のプロファイルを作り込む
- [ ] 残り4メーカーへ横展開

後回し（フォールバック経路のため優先度が下がった）:

- [ ] OCRエンジン（Tesseract）ラッパー
- [ ] スキャン画像の前処理（傾き補正・二値化・ノイズ除去）

**M1の完了条件:** 全メーカーについて、必須項目が抽出できている。

### M2: プロトタイプ（Phase 1・単独運用）

- [ ] SQLite + SQLAlchemy のデータ層
- [ ] **デバイス台帳**（登録・編集・一覧、遠隔あり/なしの絞り込み）
- [ ] **台帳のCSV一括取込**（初期データ投入。112台超を手入力させない）
- [ ] FastAPI + 画面（一括取り込み / 確認・修正 / カルテ転記 / 台帳 / 外来準備シート）
- [ ] 確認画面での元PDFハイライト表示
- [ ] 前回値との差分表示（スパークライン、変化率、変化サマリ、変化項目のみ絞り込み）
- [ ] 項目ごとの変化しきい値の設定機能（`dir` / `tol`）
- [ ] カルテ転記用テキスト生成（テンプレート編集可能）
- [ ] 外来準備シート（患者IDの一括貼り付け → 台帳照合 → ソート・印刷）
- [ ] PyInstaller + Inno Setup でインストーラー化（**Windows実機が必要**）

**台帳とCSV取込を M2 の最初に持ってくる。**
抽出機能ができる前から「一覧が引ける」という価値が出るため、早い段階で実物を触れる。

後回し候補（Phase 2）:

- [ ] リコール・安全性情報の該当患者検索（台帳が揃ってからでよい。索引だけ先に張る）

**実装しないもの**（単独運用のため Phase 2 へ後送り）:
認証・権限管理、同時アクセス制御、監査ログ、PostgreSQL、推移グラフ。

**M2の完了条件:** 自分のWindows PC 1台にインストールし、実業務で試行できる。

### M3: 現場試行

- [ ] 実業務での並行運用（従来の手作業と併走し、時間短縮効果と誤りを実測）
- [ ] 修正履歴（`field_edits`）を分析し、抽出ルールを改善
- [ ] 転記時間の削減効果を数値化（D のベースラインと比較）

**この段階で初めて、投資を続ける価値があるかを判断できる。**

### M4: 共有システム化（Phase 2）

- [ ] PostgreSQLへ移行
- [ ] 認証・権限・監査ログ
- [ ] 推移グラフ、アラート、未確認レポート一覧
- [ ] 院内サーバーへの配備

## 6.3 現時点での主なリスク

| リスク | 深刻度 | 対応 |
|---|---|---|
| ~~スキャン品質が悪くOCR精度が実用水準に届かない~~ | ~~高~~ | **解消済み。** PDFを直接取得できる |
| ~~USB持ち込みが院内規程で認められない~~ | ~~高~~ | **解消済み。** 院内専用USBを使用 |
| **患者IDの先頭ゼロが消えて患者を取り違える** | 高 | DBは`TEXT`、Excel経由のCSVを避ける、取込時に7桁を検証。[CLAUDE.md](../CLAUDE.md) に明記 |
| 一部メーカーのPDFが画像ベースだった | 中 | そのメーカーのみOCR経路を作る |
| 帳票の項目名がメーカー間で想定以上にばらつく | 中 | プロファイルYAMLで吸収。ラベル一覧を早期に集めて実態を把握する |
| 抽出項目が固まらず手戻りする | 中 | EAVスキーマで項目追加のコストを下げてある |
| Windows実機ビルドで詰まる | 中 | M2の早い段階でHello World相当のexeを1本通しておく |
| 台帳の初期データ投入が重すぎて頓挫する | 中 | CSV取込を先に作る。既存Excelの有無を早めに確認（A-3） |
| 開発が属人化して継続できなくなる | 中 | ドキュメントとテストを維持。プロファイルYAMLは非エンジニアでも読める形式にしてある |
| 商用製品のほうが早くて安いと後で判明する | 中 | M3の効果測定で正直に比較する。撤退も選択肢に残す |

## 6.4 判断のチェックポイント

無闇に進めず、以下で立ち止まって判断する。

1. **M1完了時**: 抽出は実用に足るか。足りなければ方針変更 or 中止
2. **M3完了時**: 転記時間は実際に短縮されたか。商用製品と比べて優位はあるか
3. **M4着手前**: 院内サーバーを立てる承認が得られるか
